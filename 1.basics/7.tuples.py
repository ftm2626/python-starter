# tuple: a collection with is ordered and unchangeable, used to group togther related data

student = ("jasi",26,"female")

count_name = student.count("jasi") 
find_index = student.index("female")

for x in student:
    print(x)